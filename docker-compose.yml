version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: projects_backend
    ports:
      - "8001:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/projects.db
      - PAYSTACK_SECRET_KEY=sk_test_5b74719d473192871ad566ec86db159c2ea0a4bf
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123
      - JWT_SECRET=supersecretjwtkeyforprojectspaymenttracker
      - ACCESS_TOKEN_EXPIRE_MINUTES=1440
      - RESEND_API_KEY=${RESEND_API_KEY:-re_mock_key}
      - RESEND_FROM_EMAIL=${RESEND_FROM_EMAIL:-onboarding@resend.dev}
      - ADMIN_NOTIFICATION_EMAIL=${ADMIN_NOTIFICATION_EMAIL:-admin@example.com}
    restart: always
